export const constants = {
    maxAdventureTrackSize: 7680, // exceeding this value will cause the mapbox map not to render fully
    defaultBackgroundColor: '#eeeeee',
    defaultTextColor: '#000000',
    defaultFont: 'Sanchez'
} as const;